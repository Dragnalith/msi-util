name: build

on:
  workflow_dispatch:
  workflow_call:
    secrets:
      BUILDBUDDY_API_KEY:
        required: true
    outputs:
      artifacts:
        description: "Build artifact URL"
        value: ${{ jobs.build.outputs.artifact-url }}

permissions:
  contents: read
  actions: write

jobs:
  build:
    name: Build
    runs-on: macos-latest
    outputs:
      artifact-url: ${{ steps.upload.outputs.artifact-url }}
    steps:
      - uses: actions/checkout@v4
      - name: bazel build //:for_all_platforms
        env:
          BUILDBUDDY_API_KEY: ${{ secrets.BUILDBUDDY_API_KEY }}
        run: |
          bazel build \
            --config release \
            --config remote \
            --remote_header=x-buildbuddy-api-key=$BUILDBUDDY_API_KEY \
            //:for_all_platforms
          mv bazel-out/linux_amd64_musl-opt/bin/msiutil msiutil_linux_amd64
          mv bazel-out/linux_arm64_musl-opt/bin/msiutil msiutil_linux_arm64
          mv bazel-out/macos_amd64-opt/bin/msiutil msiutil_darwin_amd64
          mv bazel-out/macos_arm64-opt/bin/msiutil msiutil_darwin_arm64
          mv bazel-out/windows_amd64-opt/bin/msiutil.exe msiutil_windows_amd64.exe
          mv bazel-out/windows_arm64-opt/bin/msiutil.exe msiutil_windows_arm64.exe
      - uses: actions/upload-artifact@v4
        id: upload
        with:
          name: artifacts
          retention-days: 1
          path: |
            msiutil_darwin_amd64
            msiutil_darwin_arm64
            msiutil_linux_amd64
            msiutil_linux_arm64
            msiutil_windows_amd64.exe
            msiutil_windows_arm64.exe
